spring:
  application:
    name: speakfit-backend

  jackson:
    time-zone: Asia/Seoul
    json:
      read:
        allow-unescaped-control-chars: true

logging:
  level:
    root: info

app:
  cookie:
    secure: false
  websocket:
    base-url: ${WS_BASE_URL:ws://localhost:8080/ws/practice/}
